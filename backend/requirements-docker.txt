# Konteyner o'rnatadigan kutubxonalar.
#
# requirements.txt -- bu `pip freeze` chiqargan to'liq ro'yxat, Windows
# ishlab chiqish mashinasi uchun. Unda hech qayerda ishlatilmaydigan
# kutubxonalar ham bor: dlib, face-recognition, Flask, alembic -- loyiha
# insightface ga o'tishidan oldingi davrdan qolgan. dlib ni Docker'da
# yig'ish o'n daqiqadan oshadi va tez-tez xato beradi, ishlatilmaydigan
# kutubxona uchun esa buning ma'nosi yo'q.
#
# Bu yerda faqat kodda rostdan import qilinadigan narsalar bor. Qolganini
# pip o'zi bog'liqlik sifatida olib keladi (masalan insightface uchun
# scipy va scikit-image).

# Web
fastapi==0.136.1
uvicorn==0.47.0
websockets==16.0
python-multipart==0.0.28
pydantic==2.13.4
email-validator==2.3.0

# Baza
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12

# Xavfsizlik
PyJWT==2.12.1
passlib==1.7.4
bcrypt==4.0.1

# Yuz tanish.
#
# opencv bu yerda ataylab yozilmagan: uni insightface o'zi bilan olib
# keladi, va aynan `opencv-python` ni -- headless variantini emas.
#
# Avval headless yozilgan edi ("serverda ekran yo'q, cv2.imshow ham yo'q,
# demak X11 kutubxonalari ortiqcha"). Mantiq to'g'ri, natija esa noto'g'ri:
# pip ikkalasini ham o'rnatdi, ular bitta cv2/ papkasiga yozadi va
# keyingisi oldingisini bosib ketadi. Konteyner ishga tushganda
# `ImportError: libxcb.so.1` bilan qulab tushdi.
#
# Bitta opencv bo'lgani -- ikkitasidan yaxshiroq. Kerakli tizim
# kutubxonalari Dockerfile da o'rnatiladi.
insightface==1.0.1
onnxruntime==1.26.0
numpy==2.4.5

# Tashqi so'rovlar: ochiq serverga sinxronizatsiya, Gemini, SMS.
httpx==0.28.1
requests==2.34.2
python-dotenv==1.2.2

# Push bildirishnomalar -- ixtiyoriy. Kod uni try/except ichida import
# qiladi va kalitsiz ham ishlayveradi, lekin Windows'dagi xulq-atvor bilan
# bir xil bo'lishi uchun shu yerda qoldirildi.
firebase_admin==7.4.0
